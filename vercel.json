{
  "builds": [
    {
      "src": "package.json",
      "use": "@vercel/static-build",
      "config": {
        "distDir": "build"
      }
    }
  ],
  "rewrites": [
    { "source": "/game", "destination": "/game" },
    { "source": "/(.*)", "destination": "/" }
  ]
}
