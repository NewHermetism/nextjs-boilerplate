# TODO LIST

## Done

- [x] Add test mode toggle and plumbing
- [x] New controls: click + SPACE/UP trigger jump

## ROADMAP TO THE EDITOR MODE. 

### REFACTOR>


I. [x] Define data-driven config

   - [x] Create `src/games/dino/config/data/{characters,environments}.json`
   - [x] Add TS normalizers (`characters.config.ts`, `environments.config.ts`, asset
    key helpers)
   - [x] Update current consumers (CharacterModal, Environment/Obstacle manager,
    DinoCharacter, PreloadScene) to rely on the config layer


II. [x] Backend contract update

   - [x] API returns `profile.characters`, `profile.environments`, `selectedCharacterId`,
    `selectedEnvironmentId`
   - [x] Socket events accept textual IDs (`character_id`, `environment_id`) instead
    of indexes
   - [x] Expose catalog endpoint for the future store/editor UI


III. [x] Asset re-organization and naming

   - [x] Rename `public/assets/envoirements` to `public/assets/environments/
    {environment-id}/...`
   - [x] Split `public/assets/objects` into `ui/buttons`, `ui/icons`, `shared`
   - [x] Move obstacle sprites under each environment folder
   - [x] Update `PreloadScene` to auto-load assets from config definitions


VI. [x] UI and gameplay updates

   - [x] Character selector iterates config JSON (no hardcoded arrays)
   - [x] Introduce map selector UI linked to unlocked environments
   - [x] Prepare “Editor Mode” scaffolding (form/editor to modify config data)


## USERNAMES AND NEW LEADERBOARD. 

## Notes
- Asset naming convention: `character.<id>.<state>`, `environment.<id>.<layer>`,
    `ui.button.<name>`
- Characters and environments stay decoupled but can reference `defaultEnvironmentId` for
    legacy linking
- Store UX: NFT purchase happens on-chain; client only refreshes profile flags to unlock
    content








